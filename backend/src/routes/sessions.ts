import { Router } from 'express';

const router = Router();

// Placeholder — session log endpoints
// POST /api/sessions — log a focus session
// GET  /api/sessions — list sessions for user

export default router;
