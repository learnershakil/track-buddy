import { Router } from 'express';

const router = Router();

// Placeholder — bridge transaction endpoints
// POST /api/bridge/initiate — start crypto-to-UPI payout
// GET  /api/bridge/status/:id — check bridge tx status
// POST /api/bridge/webhook — provider settlement callback

export default router;
