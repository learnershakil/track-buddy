import { Router } from 'express';

const router = Router();

// Placeholder — AI report endpoints
// POST /api/ai/analyze — generate discipline analysis
// POST /api/ai/predict — generate future predictions
// GET  /api/ai/reports — list reports

export default router;
