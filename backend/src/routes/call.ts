import { Router } from 'express';

const router = Router();

// Placeholder — Twilio call endpoints
// POST /api/call/trigger — trigger AI accountability call
// POST /api/call/webhook — Twilio status callback

export default router;
