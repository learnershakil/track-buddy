import { Router } from 'express';

const router = Router();

// Placeholder — commitment endpoints added in Commit 17
// POST /api/commitments — create commitment with on-chain tx
// GET  /api/commitments — list user commitments
// GET  /api/commitments/:id — get commitment detail

export default router;
